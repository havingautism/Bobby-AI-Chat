name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      # 关键改动在这里！
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "pnpm"
          # 我们暂时移除了 cache: 'pnpm' 这一行，
          # 因为 pnpm-lock.yaml 文件已被删除，无法进行缓存。

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm run build:mobile
        env:
          GENERATE_SOURCEMAP: false
          INLINE_RUNTIME_CHUNK: false
          IMAGE_INLINE_SIZE_LIMIT: 1000
          REACT_APP_OPTIMIZE_IMAGES: true
          REACT_APP_ENABLE_CODE_SPLITTING: true

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Update Service Worker Version
        run: |
          # 生成基于commit hash的版本号
          VERSION=$(git rev-parse --short HEAD)
          sed -i "s/bobby-ai-chat-v[0-9]/bobby-ai-chat-v$VERSION/g" build/service-worker.js
          
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "./build"

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
